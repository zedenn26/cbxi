'use client';
import {useState,type FormEvent} from 'react';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const d=await r.json();if(!r.ok)throw new Error(d.error);window.location.href='/admin';}catch(e){setError(e instanceof Error?e.message:'Unable to sign in');}finally{setBusy(false);}}return <main className="section"><div className="admin-login"><span className="eyebrow">Cubixtop / Administration</span><h1 className="section-title">Welcome back.</h1><p className="mb-6 text-gray-500">Sign in to manage your website.</p><form className="enquiry-form" onSubmit={submit}><label>Admin password<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p role="alert" className="form-error">{error}</p>}<button className="btn" disabled={busy}>{busy?'Signing in…':'Sign in →'}</button></form></div></main>}
