import {Target,Compass,HeartHandshake,ClipboardCheck,MessagesSquare,Laptop,ArrowUpRight} from 'lucide-react';
import {Content} from './Cms';
import Link from 'next/link';
import values from '@/lib/company-content.json';
const icons=[Target,Compass,HeartHandshake,ClipboardCheck,MessagesSquare,Laptop];
export default function CompanyPrinciples({compact=false}:{compact?:boolean}){const shown=compact?values.slice(0,2):values;return <section className="section company-principles"><div className="wrap"><div className="section-heading"><div><span className="eyebrow"><Content id="company.eyebrow">What we stand for</Content></span><h2 className="section-title"><Content id="company.heading">Your goals. Our commitment.</Content></h2></div>{compact&&<Link href="/about#our-values" className="text-link dark-link">Our values & working culture <ArrowUpRight size={20}/></Link>}</div><div id={compact?undefined:'our-values'} className={compact?'principle-cards compact':'principle-cards'}>{shown.map((item,i)=>{const Icon=icons[i];return <article key={item.id} className={'principle-card principle-'+item.id}><Icon size={29} strokeWidth={1.5}/><h3><Content id={'company.'+item.id+'.title'}>{item.title}</Content></h3><p><Content id={'company.'+item.id+'.text'}>{item.text}</Content></p>{item.id==='remote'&&<div className="remote-label">Work from home · Collaborate as one team</div>}</article>;})}</div></div></section>}
